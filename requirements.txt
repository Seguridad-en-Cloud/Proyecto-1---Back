# LiveMenu API runtime requirements (no dev tools).
# Dev/test deps live in pyproject.toml under [project.optional-dependencies].

# Core framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0
jinja2>=3.1.0

# Database
SQLAlchemy>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0

# Utilities
python-multipart>=0.0.6
structlog>=24.1.0
slowapi>=0.1.9

# Image processing & legacy storage (S3/MinIO for local dev)
Pillow>=10.2.0
boto3>=1.34.0
qrcode[pil]>=7.4.0

# GCP integrations (Secret Manager, Cloud Storage, Cloud SQL Connector).
# These are imported lazily at runtime so they do nothing on a local
# docker-compose deployment that doesn't set GCP_PROJECT_ID.
google-cloud-secret-manager>=2.20.0
google-cloud-storage>=2.18.0
cloud-sql-python-connector[asyncpg]>=1.11.0
